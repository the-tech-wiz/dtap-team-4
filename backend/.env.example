# Port for the REST API
PORT=3000

# URL for the local Mosquitto broker
# Since broker is on the same VM, use localhost
MQTT_URL=mqtt://localhost:1883

# Leave blank to allow anonymous connections (fine for initial testing)
# Add credentials once you enable authentication in mosquitto.conf
MQTT_USERNAME=
MQTT_PASSWORD=

NODE_ENV=production
