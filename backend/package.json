{
  "name": "mqtt-api",
  "version": "1.0.0",
  "description": "REST API + MQTT bridge for IoT device control",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "mqtt": "^5.3.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
